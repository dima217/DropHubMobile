import AccountDetails from "@/shared/AccountDetails";
import View from "@/shared/View";

const Matches = () => {
  return (
    <View>
        <AccountDetails />
    </View>
  );
};

export default Matches;
